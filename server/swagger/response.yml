components:
  schemas:
    errorResult:
      type: object
      properties:
        code:
          type: integer
          description: 에러 코드
          example: 1
        message:
          type: string
          description: 에러 메세지
          example: "invalid token"
    success301:
      type: object
      properties:
        url:
          type: string
          description: post 요청 후 리다이렉션 될 경로
          example: "/"
