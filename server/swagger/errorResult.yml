components:
  schemas:
    errorResult:
      type: object
      properties:
        code:
          type: integer
          description: 에러 코드 (only when it is failed)
          example: 1
        message:
          type: string
          description: 에러 메세지
          example: "invalid token"
